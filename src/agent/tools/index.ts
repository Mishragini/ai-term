import { deleteFile, listFiles, readFile, writeFile } from "./file.js";
import { webSearch } from "./websearch.js";

export const tools = {
    readFile,
    writeFile,
    listFiles,
    deleteFile,
    webSearch
}